plugins {
  id 'pl.allegro.tech.build.axion-release'
}

scmVersion {
  tag {
    prefix = 'v'
    versionSeparator = ''
  }
  nextVersion {
        suffix = "NEXT"
  }
}

version = scmVersion.version

